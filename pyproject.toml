[project]
name = "buckshot-lambda"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
	"speciesnet>=5.0.3",
	"opencv-python-headless",
	"setuptools<81.0.0",
]

[tool.uv]
exclude-dependencies = ["opencv-python"]
